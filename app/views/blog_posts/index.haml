.span12
  %h1 Blog
  - @blog_posts.each do |blog_post|
    %h2= link_to blog_post.title, blog_post_url(blog_post.id)
    %div= Markdown.new(truncate(blog_post.content, length: 300)).to_html.html_safe 
    %p= link_to 'READ MORE &rarr;'.html_safe, blog_post_url(blog_post.id)
